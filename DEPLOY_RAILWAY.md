# 🚂 Деплой на Railway.app (БЕЗ КАРТЫ!)

Railway.app предоставляет **$5 бесплатных кредитов в месяц** без привязки карты.

---

## 📋 Шаг 1: Backend на Railway.app

### 1.1 Создайте аккаунт
1. Перейдите на https://railway.app
2. Зарегистрируйтесь через **GitHub**
3. **НЕ требует привязки карты!**

### 1.2 Создайте новый проект
1. Нажмите **"New Project"**
2. Выберите **"Deploy from GitHub repo"**
3. Выберите ваш репозиторий `dilfwallet`

### 1.3 Создайте PostgreSQL Database
1. В проекте нажмите **"+ New"** → **"Database"** → **"Add PostgreSQL"**
2. База создастся автоматически
3. Railway автоматически создаст переменную `DATABASE_URL`

### 1.4 Настройте Backend Service
1. В проекте нажмите **"+ New"** → **"GitHub Repo"**
2. Выберите ваш репозиторий
3. Railway автоматически определит, что это Python проект
4. **Root Directory**: Укажите `backend`
5. **Start Command**: `uvicorn app.main:app --host 0.0.0.0 --port $PORT`

### 1.5 Переменные окружения
В настройках вашего сервиса → **Variables**:
```
SECRET_KEY = <сгенерируйте: openssl rand -hex 32>
ALLOWED_ORIGINS = https://your-domain.com,https://your-app.vercel.app
```
(Если `DATABASE_URL` не появился автоматически - добавьте его из настроек PostgreSQL)

### 1.6 Инициализируйте базу данных
1. Railway Dashboard → ваш сервис → **Deployments** → нажмите на последний деплой
2. Откройте **Logs**
3. После успешного деплоя, откройте **Shell**
4. Выполните:
   ```bash
   cd backend
   python -c "from app.init_db import init_models; import asyncio; asyncio.run(init_models())"
   ```

### 1.7 Получите URL
Railway автоматически даст вам URL типа: `https://your-app.up.railway.app`

---

## 📋 Шаг 2: Frontend на Vercel (бесплатно, без карты)

1. Перейдите на https://vercel.com
2. **Add New Project** → выберите репозиторий
3. **Root Directory**: `frontend`
4. **Environment Variable**:
   ```
   NEXT_PUBLIC_API_BASE_URL = https://your-app.up.railway.app
   ```
5. **Deploy**

---

## 📋 Шаг 3: Подключение домена

### 3.1 Railway (Backend)
1. В вашем сервисе: **Settings** → **Networking**
2. Нажмите **"Generate Domain"** или добавьте Custom Domain
3. Обновите `ALLOWED_ORIGINS` с вашим доменом

### 3.2 Vercel (Frontend)
1. **Settings** → **Domains**
2. Добавьте ваш домен
3. Следуйте инструкциям для DNS

---

## 💰 Стоимость

- **Railway**: $5 бесплатных кредитов/месяц (достаточно для MVP)
- **Vercel**: Полностью бесплатно для Next.js
- **Всего**: $0 при использовании бесплатных лимитов

---

## ✅ Преимущества Railway

✅ НЕ требует карту  
✅ Простой деплой  
✅ Автоматический PostgreSQL  
✅ Свой домен бесплатно  
✅ Логи и мониторинг  

---

## 🔧 Если нужен Firebase

Если вы хотите использовать Firebase, то:
- **Frontend**: можно задеплоить на Firebase Hosting (бесплатно)
- **Backend**: нужно переписать на Firebase Cloud Functions (Node.js) или использовать Cloud Run (Docker)

Но это **не оптимально** для вашего Python FastAPI backend.

---

**Рекомендация**: Используйте **Railway.app + Vercel** — это самый простой и бесплатный вариант без привязки карты!

